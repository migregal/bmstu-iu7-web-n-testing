[pytest]

addopts = -v --color=yes --showlocals -rxXs --alluredir=../../.allure_results

pythonpath = ../python

log_level=INFO
log_format = %(asctime)s %(levelname)s %(name)s: %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
